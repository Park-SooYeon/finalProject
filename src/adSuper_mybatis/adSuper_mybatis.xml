<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTDMapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="admin">
	
	<select id="select_member" resultType="bean.membershipVo">
		select 	member_name,
				member_id, 
				phone,
				email, 
				mdate, 
				state
				from membership
				order by mdate desc
	</select>
	 
	<select id="view_member" parameterType="String" resultType="bean.membershipVo">
		select 	member_id, 
				member_name, 
				phone, 
				email,  
				state, 
				mdate
				from membership 
				where membership_member_id = ${_parameter}
	</select> 
</mapper>